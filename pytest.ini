[pytest]
testpaths = product/tests
norecursedirs = .venv .venv312 venv __pycache__ *.egg-info product/audio_preprocessing/data
addopts = -v --tb=short -m "not slow"
markers =
    slow: requires pretrained weight downloads (~500 MB, needs internet)
    integration: end-to-end pipeline tests
